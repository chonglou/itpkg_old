#set(SessionItem gl_session=session.d1s7e0wp)
#set(Company company)
#if(gl_session.ssCompanyManager)
<script type="text/javascript">
$(document).ready(function () {
    new Ajax("/company/manage/info");
});
</script>
#else
<fieldset>
    <legend>${company.name}</legend>
    ${company.details}
</fieldset>
<br/>
#end