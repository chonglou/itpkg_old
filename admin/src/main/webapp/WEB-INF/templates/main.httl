$!{extends("/layout.httl")}

#macro(main)
<fieldset>
    #set(List<String> logList)
    <legend>更新日志[${logList.size}]</legend>
    <ul>
    #for(i : logList)
        <li>${i}</li>
    #end
    </ul>
</fieldset>
#end