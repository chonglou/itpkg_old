$!{extends("/layout.httl")}

#macro(main)
<fieldset>
    #set(List<String> logList)
    <legend>大事记[${logList.size}]</legend>
    <ul>
    #for(i : logList)
        <li>${i}</li>
    #end
    </ul>
</fieldset>
#end