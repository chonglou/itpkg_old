- @nav_items = repositories_nav_buttons

h3 #{t 'links.repository.clone'}
hr
pre
  code
    git clone #{@clone_url}
|OR
pre
  code ssh-agent bash -c 'ssh-add YOUR_KEY_FILE; git clone #{@clone_url}'
p &nbsp;

h3 #{t 'links.repository.branch.list'}
hr
= render 'layouts/item_list', items:@branches