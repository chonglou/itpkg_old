- @title=t('links.vpn_host.show', name:@host.name)

= render 'layouts/button_group', items:@buttons, back:false

h3 #{t('links.vpn_host.remove', name:@host.name)}
hr
= render 'layouts/remove_button', name:'buttons.remove', url:vpn_host_path(@host)
