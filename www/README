配置文件在：web.cfg
app_secret需要16位

默认数据存储在tmp目录下

密钥可以使用"pwgen -n 16"生成

需要中文字体文件在tmp/font.ttc(验证码需要)

启动：
python app start

停止：
python app stop

调试：
python app debug


加载python环境：
mkdir tmp
cd tmp
virtualenv python3
cd ..
source tmp/python3/bin/activate
pacman -S tcl tk
pip install Pillow
pip install tornado SQLAlchemy pycurl pycrypto redis WTForms wtforms-tornado beaker qrcode markdown mysql-connector-python psutil

退出python环境：
deactivate

编辑器设定：禁止全屏
